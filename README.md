# 🎵 Beat Saber Song Converter (v2 to v1)

A powerful Python script designed for **Legacy Beat Saber**. These scripts automates the tedious process of converting modern map formats back to the original legacy format.

## ✨ Features
* **Format Conversion**: Converts `info.dat` to `info.json` and `.dat` map files to `1.5.0` `.json`.
* **Audio Fixer**: Automatically renames `.egg` files to `.ogg` and updates the internal metadata.
* **Auto-Cleanup**: Deletes old `.dat` and `.egg` files automatically after a successful conversion.
* **Compatibility**: Works perfectly with 0.13.2 and under!

## 🚀 How to Use
a.
1. Download `v1converter.py` and place it inside your Beat Saber song folder.
2. Ensure you have **Python** installed.
3. Type "cmd" in the adress bar of your song folder.
4. Run the script:
   ```bash
   python v1converter.py
b.
1. Download `multipleconverter.py` and place it inside your CustomLevels folder.
2. Ensure you have **Python** installed.
3. Type "cmd" in the adress bar of your CustomLevels folder.
4. Run the script:
   ```bash
   python multipleconverter.py
   
This will convert multiple levels at once!
